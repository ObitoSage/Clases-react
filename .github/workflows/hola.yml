name: Hola Mundo

on:
  push:
  workflow_dispatch:
jobs:
  hola:
    runs-on: ubuntu-latest
    steps:
      - name: Saludar
        run: echo "Hola mundo"
      - name: descargar codigo
        uses: actions/checkout@v4
      - name: en que carpeta estas
        run: pwd
      - name: Listado de archivos
        run: ls -la